---
const {
  title = "¿Querés sumarte a la nueva generación?",
  description = "Inscribite hoy y comenzá tu camino en la robótica educativa con LEGO y programación creativa.",
  ctaLabel = "Inscribirme ahora",
  ctaLink = "https://wa.me/+5492324468366",
} = Astro.props;
---

<section
  id="cta"
  class="section relative text-center overflow-hidden rounded-2xl bg-[var(--color-primary)]/90 dark:bg-[var(--color-secondary)]/90 text-[var(--color-black)] dark:text-[var(--color-black)] max-w-5xl mx-auto my-16 px-6 py-16 shadow-xl backdrop-blur-sm transition-colors duration-500"
  role="region"
  aria-labelledby="cta-title"
>
  <!-- Fondo animado -->
  <svg
    class="absolute inset-0 w-full h-full opacity-10 pointer-events-none animate-pulse"
    viewBox="0 0 200 200"
    xmlns="http://www.w3.org/2000/svg"
    aria-hidden="true"
  >
    <circle cx="100" cy="100" r="80" fill="currentColor"></circle>
  </svg>

  <!-- Contenido principal -->
  <div class="relative z-10">
    <h2
      id="cta-title"
      class="text-3xl sm:text-4xl font-title font-bold mb-4 leading-snug tracking-tight"
    >
      {title} 🚀
    </h2>

    <p
      class="text-base sm:text-lg opacity-90 max-w-2xl mx-auto mb-6 font-body leading-relaxed text-balance"
    >
      {description}
    </p>

    <p
      class="text-sm text-[var(--color-black)]/70 dark:text-[var(--color-black)]/70 italic mb-6"
    >
      ¡Más de 500 alumnos ya se sumaron desde 2021!
    </p>

    <a
      href={ctaLink}
      target="_blank"
      rel="noopener noreferrer"
      class="inline-flex items-center gap-2 bg-black dark:bg-white text-white dark:text-black px-8 py-4 text-base font-semibold rounded-full shadow-md hover:scale-105 hover:shadow-lg focus-visible:outline focus-visible:outline-offset-2 focus-visible:outline-black transition-transform duration-300"
    >
      <svg
        xmlns="http://www.w3.org/2000/svg"
        class="w-5 h-5"
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M12 4v16m8-8H4"></path>
      </svg>
      {ctaLabel}
    </a>
  </div>
</section>

<style>
  @keyframes pulse {
    0%,
    100% {
      transform: scale(1);
      opacity: 0.1;
    }
    50% {
      transform: scale(1.2);
      opacity: 0.2;
    }
  }

  .animate-pulse {
    animation: pulse 12s ease-in-out infinite;
  }
</style>
