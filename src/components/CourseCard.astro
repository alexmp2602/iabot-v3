---
const { icon: Icon, title, age, description, link } = Astro.props;
---

<article
  class="card flex flex-col gap-4 p-6 text-left hover:shadow-xl focus-within:ring-2 focus-within:ring-[var(--color-primary)] focus-within:ring-offset-2 transition-transform duration-300 hover:-translate-y-1"
  tabindex="0"
>
  <!-- Ícono con fondo circular -->
  <div
    class="w-12 h-12 flex items-center justify-center rounded-full bg-[var(--color-primary)]/10 text-[var(--color-primary)]"
  >
    <Icon class="w-6 h-6" />
  </div>

  <h3 class="text-xl font-semibold font-title text-[var(--color-text)]">
    {title}
  </h3>

  <p
    class="text-sm font-medium text-[var(--color-primary)] dark:text-[var(--color-secondary)]"
  >
    {age}
  </p>

  <p
    class="text-sm text-[var(--color-text)] opacity-90 font-body leading-relaxed"
  >
    {description}
  </p>

  <a
    href={link}
    class="mt-auto inline-block text-sm font-semibold text-[var(--color-primary)] dark:text-[var(--color-secondary)] hover:underline focus-visible:outline focus-visible:outline-offset-2 focus-visible:outline-[var(--color-primary)]"
  >
    Ver más →
  </a>
</article>
