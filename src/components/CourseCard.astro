---
const { icon: Icon, title, age, description, link } = Astro.props;
---

<article
  class="bg-white dark:bg-neutral-900 rounded-xl shadow-md hover:shadow-lg transition p-6 text-left flex flex-col gap-4"
>
  <!-- Ícono con fondo circular -->
  <div
    class="w-12 h-12 flex items-center justify-center rounded-full bg-primary/10 text-primary hover:bg-primary/20 transition duration-300"
  >
    <Icon class="w-6 h-6" />
  </div>

  <h3 class="text-xl font-semibold text-black dark:text-white">{title}</h3>
  <p class="text-sm text-primary font-medium">{age}</p>
  <p class="text-sm text-neutral-600 dark:text-neutral-400">{description}</p>

  <a
    href={link}
    class="mt-auto text-sm text-primary hover:text-secondary transition font-semibold"
  >
    Ver más →
  </a>
</article>
